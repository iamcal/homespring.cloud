{
  "description": "One-line cat — an empty/newline program that echoes stdin to stdout.",
  "source": "www/examples/2003-jeff-binder/cat.hs",
  "input": "hello\n",
  "tick_limit": 20,
  "timeout": 3,
  "expected_output": "hello\n",
  "overrides": {
    "2003-cal-henderson": {
      "skip": true,
      "skip_reason": "Has no input support"
    },
    "2003-jeff-binder": {
      "skip": true,
      "presumed_pass": true,
      "skip_reason": "Does echo stdin (verify with `yes hello | hs cat.hs`), but it requires a continuously-open stdin stream; the harness's piped single-shot input closes too quickly for the interpreter's poll-based reader to pick up the data."
    },
    "2012-quin-kennedy": {
      "extra_newlines": [
        6
      ]
    },
    "2017-addison-bean": {
      "notes": "Panics with `already borrowed: BorrowMutError` on the first FishDown tick — river.rs:237's move_salmon borrows the parent mutably while the tick recursion still holds it, so any multi-level tree crashes before producing output."
    },
    "2018-martijn-arts": {
      "notes": "No stdin support — input salmon are never created."
    },
    "2023-james-thistlewood": {
      "notes": "Produces no output — the driver's one-line-per-tick stdin model doesn't line up with the interpreter's input loop for this program."
    }
  }
}
