{
  "description": "One-line cat \u2014 an empty/newline program that echoes stdin to stdout.",
  "source": "www/examples/2003-jeff-binder/cat.hs",
  "input": "hello\n",
  "tick_limit": 20,
  "timeout": 3,
  "expected_output": "hello\n",
  "overrides": {
    "2003-cal-henderson": {
      "skip": true,
      "skip_reason": "Has no input support"
    },
    "2003-jeff-binder": {
      "skip": true,
      "presumed_pass": true,
      "skip_reason": "Does echo stdin (verify with `yes hello | hs cat.hs`), but it requires a continuously-open stdin stream; the harness's piped single-shot input closes too quickly for the interpreter's poll-based reader to pick up the data. Count as pass-in-spirit."
    },
    "2012-quin-kennedy": {
      "extra_newlines": [
        6
      ]
    }
  }
}
