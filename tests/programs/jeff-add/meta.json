{
  "description": "Single-digit adder \u2014 pipes two digits through a unary counter. Famously slow.",
  "source": "program.hs",
  "input": "2\n3\n",
  "tick_limit": 200,
  "timeout": 10,
  "expected_output": "? + ",
  "normalize": "prefix",
  "overrides": {
    "2003-cal-henderson": {
      "skip": true,
      "skip_reason": "Perl module cannot handle the full add program"
    },
    "2003-jeff-binder": {
      "skip": true,
      "skip_reason": "The program is interactive and slow; Jeff's Guile interpreter produces output when driven from a terminal but not through the harness's piped stdin"
    }
  }
}
