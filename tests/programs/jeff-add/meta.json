{
  "description": "Single-digit adder \u2014 pipes two digits through a unary counter. Famously slow.",
  "source": "program.hs",
  "input": "2\n3\n",
  "tick_limit": 200,
  "timeout": 10,
  "expected_output": "? + ",
  "normalize": "prefix",
  "overrides": {
    "2003-cal-henderson": {
      "skip": true,
      "skip_reason": "Cannot handle the full add program"
    },
    "2003-jeff-binder": {
      "skip": true,
      "presumed_pass": true,
      "skip_reason": "Runs this program correctly under an interactive terminal \u2014 typing `2` then `3` at the prompts produces `? + 5` \u2014 but the harness's one-shot piped stdin closes before the slow unary counter finishes. Count this as a pass-in-spirit."
    }
  }
}
