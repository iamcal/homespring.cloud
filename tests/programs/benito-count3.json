{
  "description": "Counter variant — prints underscores then an x-prefixed alphabet ramp.",
  "source": "www/examples/2013-benito-van-der-zander/count3.hs",
  "input": "",
  "tick_limit": 80,
  "timeout": 5,
  "expected_output": "________________________x_xA_xB_xC_xD_xE_xF_xG_xH_xI_0I_1I_2I_3I_4I_5I_6I_7I_9I_0H_1H_2H_3H_4H_5H_6H_7H_9H_0G_1G_2G_3G_4G_5G_6G_7G_9G_0F_1F_2F_3F_4F_5F_6F_7F_9F_0E_1E_",
  "overrides": {
    "2003-jeff-binder": {
      "skip": true,
      "skip_reason": "Raises a runtime backtrace"
    },
    "2003-cal-henderson": {
      "skip": true,
      "skip_reason": "Overflows (deep recursion)"
    },
    "2012-quin-kennedy": {
      "skip": true,
      "skip_reason": "Produces '_________9_9A_9B…' instead of '________x_xA…'"
    },
    "2017-addison-bean": {
      "notes": "Panics with `already borrowed: BorrowMutError` on the first FishDown tick — river.rs:237's move_salmon borrows the parent mutably while the tick recursion still holds it, so any multi-level tree crashes before producing output."
    },
    "2018-martijn-arts": {
      "notes": "Parser splits on spaces only, so the newline + indent structure of the source collapses into a flat tree — the intended river layout is lost and output is either empty or a stream of literal node-name salmon."
    },
    "2023-james-thistlewood": {
      "notes": "Halts partway through — emits 72 underscores then stops before reaching the lettered digits."
    }
  }
}
