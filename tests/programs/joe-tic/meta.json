{
  "description": "Tic-tac-toe — needs stdin input to start. Prints the board frames between moves.",
  "source": "program.hs",
  "input": "hello\n",
  "tick_limit": 200,
  "timeout": 8,
  "expected_output": " | |",
  "normalize": "prefix",
  "overrides": {
    "2003-jeff-binder": {
      "skip": true,
      "skip_reason": "Jeff's Scheme interpreter emits nothing for this program"
    },
    "2003-cal-henderson": {
      "skip": true,
      "skip_reason": "Perl module does not support the node types used"
    },
    "2012-quin-kennedy": {
      "skip": true,
      "skip_reason": "Quin's interpreter emits nothing for this program"
    }
  }
}
